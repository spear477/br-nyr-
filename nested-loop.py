def nested_loop():
    
    no_stu = eval(input("Type no of students -"))
    no_sub = eval(input("Type no of subjects -"))
    
    for i in range(no_stu):
        for j in range(no_sub):
            score = eval(input(" "+str(j+1)+" "))
            
            
            
        
    

def main():
    # moredata = "yes"
    # _sum = 0.0
    # count = 0
    # while moredata[0] == 'y':
    #     x = eval(input("Enter a number >> "))
    #     _sum = _sum + x
    #     count = count + 1
    #     moredata = input("Do you have more numbers (yes or no)? ")
    nested_loop()
